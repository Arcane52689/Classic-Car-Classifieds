<a class="logo" href="#"> Classic Car Classifieds </a>

<% if (user.loggedIn()) { %>
  <label class="hello"> Hello, <%= user.escape("email") %> </label>
<% } %>

<ul class="links group">
  <% if (!user.loggedIn()) { %>
    <li><button class="login nav-link">Login</button></li>
    <li><button class="signup nav-link"> Sign Up </button> </li>
  <% } else { %>
    <li>  <button class="logout nav-link">Logout</button> </li>
    <li> <button class="my-listings nav-link">My Listings </li>
  <% } %>

</ul>
